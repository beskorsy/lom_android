<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="@color/colorPrimary"
             tools:context="lom.lom_android.view.FullscreenActivity">

    <ImageView android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               android:src="@drawable/logo_vector"/>

    <TextView android:id="@+id/errorMsg"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:layout_marginTop="-330dp"
              android:keepScreenOn="true"
              android:textColor="@color/white"
              android:textStyle="bold"
              android:textSize="24sp"
              android:gravity="center"
              android:text="Проблемы с подключением к серверу. Попробуйте еще раз."
              android:visibility="invisible"/>
    <Button android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/retryBtn"
            android:text="Повторить"
            android:layout_gravity="center"
            android:layout_marginTop="170dp"
            android:visibility="invisible"
            android:onClick="onClickSubmitBtn"/>
</FrameLayout>
